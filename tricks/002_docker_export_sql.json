{
    "data": {
      "name": "Exportar la base de datos Docker-Postgresql",
      "desc": "Exportar desde un contenedor de Docker, con postgresql la base de datos a una salida estandar con SQL",
      "code": ["~# docker exec -t container_name pg_dumpall -c -U postgres"]
    }
  }